let assert = require('assert');

describe('Test', function () {
	describe('Description', function () {
		
		before(async function () {
      // Some before action.
		});

		it('Should debug into this!', async function () {
			assert.equal(0, 0);
		});

	});
});
